# prettymd

**prettymd** is an AI-powered Markdown formatter designed for developer workflows.
It improves existing `.md` files by polishing clarity, consistency, grammar, and tone — all from the command line.

Think of it as **"Prettier for Markdown, powered by AI."**

> ⚠️ Early MVP — expect rough edges.
> This tool is under active development.

---

## 🚀 Quick Start

### 1. Install / Build

Clone the repository and build the executable:

```bash
git clone https://github.com/alexissan/prettymd
cd prettymd
swift build -c release
cp .build/release/prettymd /usr/local/bin/prettymd
```